# Runtime dependencies for the CodeSage backend.
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0
sqlalchemy==2.0.54
alembic==1.20.0
psycopg[binary]==3.3.5
# Source-code parsing (Phase 3). Grammars ship as prebuilt wheels; no compiler needed.
# 0.26.0 segfaults while analyzing larger files (native crash in the binding); 0.25.2 is stable.
tree-sitter==0.25.2
tree-sitter-python==0.25.0
tree-sitter-javascript==0.25.0
tree-sitter-typescript==0.23.2

# Retrieval (Phase 4). fastembed runs BGE embeddings and cross-encoder rerankers
# locally through ONNX Runtime — far smaller than a PyTorch install and identical
# to set up on every OS. FAISS stores the dense vectors.
fastembed==0.8.0
faiss-cpu==1.15.1
numpy==2.3.4
